# CHANGE

## 2026-04-17 凌晨追加 2

### 这次把 `Start-Poptropica-Flash.bat` 收成真正的一键启动了
- 现在双击这个批处理文件，会直接拉起 `POPTROPICA_FLASH` 主窗口。
- 我已经自己本地测过了：
  - `cmd` 脚本会立刻返回
  - 主程序窗口会起来
  - 不再依赖那层隐藏 `PowerShell` 去转发启动

### 现在启动顺序更简单
- 优先直接用项目自带的 `electron.exe` 启动主界面
- 只有在本地 `Electron` 不存在时，才回退到 `node tools/launch.js`

### 这次顺手一起处理了什么
- 启动前还是会照常清理旧的残留进程
- 但不再走之前那种“PowerShell 再套一层 Start-Process”的链
- 所以这次更像一个正常软件入口，而不是脚本套脚本

### 你现在该怎么用
- 直接双击：
  - `Start-Poptropica-Flash.bat`
- 如果你想完全不看黑框，也还是可以继续双击：
  - `Start-Poptropica-Flash.vbs`

## 2026-04-17 凌晨追加 1

### 这次把 `PdfJs.jsm` 和 `GMPInstallManager` 那两条烦人的浏览器报错修了
- 你刚才贴出来的这两条报错，根因都不在游戏脚本，而是在 `Flashpoint Navigator` 自己的 Firefox profile：
  - `PdfJs.jsm` 那条，是某个本来应该是“字符串”的浏览器偏好值，被写成了数字
  - `GMPInstallManager` 那条，是浏览器里一个插件更新地址被写成了空字符串

### 这次已经直接改掉的内容
- 我已经把当前浏览器 profile 里的坏偏好值修正了：
  - `plugin.disable_full_page_plugin_for_types` 改回了正确的字符串
  - `pdfjs.disabled` 直接关掉，避免它继续掺和旧版游戏运行
  - `media.gmp-manager.updateEnabled` 关掉
  - `media.gmp-provider.enabled` 关掉
  - `media.gmp-manager.url` 改成了合法的本地占位地址，不再是空字符串

### 这次不是只修当前一次
- 我还把启动链也一起改了。
- 以后每次启动 `Flashpoint Navigator` 之前，都会自动把这些浏览器偏好修成正确值。
- 所以这类错误不应该反复回来。

### 这次修的是什么，不是什么
- 这一步修的是：
  - 浏览器层的报错噪音
  - 启动时 Firefox 自己的异常日志
- 这一步不是：
  - 直接修好所有岛屿汉化
  - 直接修好游戏声音或静态招牌

### 你下一次看到的应该是什么
- 再启动游戏时，不该再出现你贴的那两条 `PdfJs.jsm` / `GMPInstallManager` 错误
- 如果后面还有新的报错，那就是别的链路，不是这次这一组旧浏览器偏好问题了

## 2026-04-16 晚上追加 5

### 这次先把 `Super Power` 对话气泡最痛的那个问题继续往前修了
- 你刚才那两张图说明得很清楚：
  - 店里有些中文已经能正常显示，
  - 但主街 NPC 一说话，就会吐出一长串十六进制垃圾，
  - 所以这不是“所有中文字体都坏了”，而是 `AS2` 的对话气泡链单独坏了。

### 这次我新查清的根因
- `Super Power` 旧版 `AS2` 的人物对话，不只是把中文写进场景脚本就完事。
- 角色真正说话时，还会额外去加载：
  - `popups/balloon.swf`
- 之前这个资源在当前 `AS2` 运行包里是缺的，所以对话显示链一直不完整。

### 这次我已经实际补上的东西
- 我没有继续空转去硬撬整个 `char.swf` 根脚本。
- 我改成更稳的路：
  - 直接用一个可控的 `balloon.swf` 替掉缺失的对话气泡资源。
- 这个新的 `balloon.swf` 现在已经能做三件事：
  - 把 `%uXXXX` 这种中文安全写法还原成真中文
  - 把那种连续十六进制垃圾串还原成正常文本
  - 自己画出白色对话气泡，不再依赖那条坏掉的旧链

### 这次已经写进实际运行包
- 不是只放在临时目录里。
- 现在新的对话气泡资源已经写进：
  - `as2-runtime.zip`
- 下次重新挂载后，`Super Power` 会直接吃到这条新对话气泡链。

### 我顺手把“画面太小”这件事也继续往前推了
- 这次已经开始改 `AS2` 的 `base.php`：
  - 不再只是页面整体放大，
  - 而是开始按窗口大小去缩放真实游戏舞台。
- 这一步已经写回 `as2-runtime.zip`，不过还要你下一次实际重开后看最终效果。

### 你下一次该怎么测
- 先把所有旧的 `Flashpoint Navigator` 窗口彻底关掉。
- 然后重新运行：
  - `npm run launch -- --island super-power`
- 这次重点只看三件事：
  - 主街 NPC 说话时，还会不会再出现那串十六进制垃圾
  - 游戏画面有没有比之前更接近“铺满窗口”
  - 店里原来已经好的中文有没有退化

### 还没收尾的部分我继续盯着
- 主街的大招牌、雨棚字、门上的 `OPEN`
- 这些静态字还没有真正完全变成中文
- 下一步我会继续只盯 `Super Power`，把这批静态字也逐个补掉

## 2026-04-16 晚上追加 4

### 这次我把 `Super Power` 主街那层“看不见的中文”继续往前修了
- 你这次反馈得很准：
  - 店里小牌子能变中文，
  - 但主街大招牌、雨棚字、门牌还是英文，
  - 说明不是“整个中文字体都没了”，而是 `sceneSuperMain.swf` 这一层还没真正安全显示。

### 这次新确认的根因
- `Super Power` 主街这层不是普通外部文本。
- 它主要还是 `AS2` 脚本和场景层。
- 之前直接把中文原样塞进脚本，容易出现一种很坑的状态：
  - 包里看起来已经有中文，
  - 但真正运行时会因为编码问题，变成空白或者根本不显示。

### 这次我改成了更稳的写法
- 我把 `sceneSuperMain` 里这批中文脚本改成了 `unescape("%uXXXX")` 这种纯 ASCII 形式。
- 这样做的目的很简单：
  - 避开旧 `AS2` 脚本里对中文直写的不稳定编码，
  - 让运行时自己还原成真正的 Unicode 中文。

### 这次已经实际做掉的事
- `sceneSuperMain` 里那批主街脚本已经重新打包。
- 新包不只是临时文件，我已经把它重新写回了：
  - `as2-runtime.zip`
  - 当前挂载中的实际游戏 zip

### 你现在应该怎么测
- 先把所有旧的 `Flashpoint Navigator` 窗口完全关掉。
- 然后重新运行：
  - `npm run launch -- --island super-power`
- 这次重点先看：
  - 主街最显眼的招牌字
  - 主街 NPC 气泡对白
  - 以及进店后原来已经变中文的小牌子有没有退化

### 还有一件事我已经承认并单列了
- 现在窗口太小、缩放不像真正放大游戏，这个问题还没修完。
- 目前浏览器放大只是在放页面，不是把旧版游戏真正全屏化。
- 下一步会单独改“窗口与画面缩放”这条链，不和汉化继续混着修。

## 2026-04-16 晚上追加 3

### 这次把 `Super Power` 还是英文的根因彻底查清了
- 之前不是“运行包没挂上”，也不是“根本没翻译”。
- 真问题是：
  - 这岛里很多真正上屏的对白不在普通文本里，
  - 而是在 `AS2` 的脚本字符串里。
- 之前那套汉化链只抓到了普通文本，没抓到这层脚本对白。

### 现在补上的新能力
- `AS2` 的 `SWF` 现在不只会导出普通文本，也会导出脚本。
- 项目现在能把这类脚本对白重新写回 `SWF`。
- 这一步已经先在 `Super Power` 上跑通了。

### 这次 `Super Power` 真实发生了什么变化
- 重提取后，这岛从原来几乎抓不到主街对白，变成抓到 `189` 条文本。
- 其中主街 `sceneSuperMain.swf` 现在能看到真正的脚本对白条目。
- 我又把这些条目翻译并重新打进了实际运行包。

### 我已经直接从正在使用的运行包里验过
- 不是只看数据库，也不是只看清单。
- 我直接从 `as2-runtime.zip` 里把 `sceneSuperMain.swf` 抽出来重新导出脚本看了。
- 里面现在已经能看到中文，比如：
  - `监狱发生越狱了! 我们都有危险!`
  - `我把岛奖章交给了我们的英雄， 内德·面条头！`
  - `记住， 能力越大， 责任越大.`

### 现在还没一起变成中文的是什么
- 这类静态招牌和场景字还没有一起变：
  - `SHOP`
  - `COMIC SHOP`
  - `OPEN`
  - `THE DAILY PAPER`
- 原因也已经确认了：
  - 这些不是普通对白，
  - 也不是刚才那层脚本字符串，
  - 而是更偏静态字形 / 图形资源。

### 这一轮最诚实的结果
- `Super Power` 的**对白层**现在已经正式进入中文注入链。
- 你之前那种“气泡对白还是英文”的问题，这次已经不是旧状态了。
- 但场景里的静态招牌字，还要继续做下一步专门处理。

## 2026-04-14

### 2026-04-15 这轮最重要的真实结果
- `AS3` 这轮我已经把**全量翻译**真正跑完了。
- 现在数据库里的 `AS3` 文本状态是：
  - `73577 / 73577` 都已经有翻译
- 也就是说：
  - 不是“还没开始翻”
  - 也不是“只翻了一小部分”
  - 而是 **AS3 的文本翻译这一步已经全量跑完**

### 这轮我还把整包打包链真正跑完了
- 这次不是只写数据库。
- 我把 `AS3` 的完整打包链也重新跑完了。
- 现在新的 `as3 manifest` 已经重新生成出来了。
- 当前打包结果里，`AS3` 已经有：
  - `5726` 个资产被写进中文包

### 我亲自重新做了真实启动验收
- 这次我不是只看命令返回成功。
- 我重新做了真实启动、截图、OCR、音频和弹窗审计。
- 当前真实结果是：
  - `AS3` 能稳定进入旧版游戏窗口
  - 这次音频检测重新是 **有活动**
  - 没有复现 `cmd / powershell` 这类黑框弹窗
  - 首屏画面已经恢复正常，不再是纯蓝屏或黑屏

### 现在屏幕上实际看到的是什么
- 当前 `AS3` 首屏又回到了正常可见状态。
- 现在能看到：
  - `NABOOTI`
  - `SPY`
  - `SUPER POWER`
  - `EARLY POPTROPICA`
  - 底部那句商店提示
- 这说明：
  - 运行包没有继续坏掉
  - 首屏确实能正常渲染

### 但最关键的问题还没过线
- 虽然 `AS3` 文本已经全量翻完，包也重新打好了，
- **屏幕上这批首屏可见英文还没有真正变成中文**

### 这轮新确认出的根因
- 现在可以更明确地说：
  - 不是“没翻”
  - 也不是“没打包”
- 当前卡住的是：
  - 首屏这几批最显眼的英文，**并不走普通文本替换就能生效的那一层**
  - 它们要么是更老的 `SWF` 文本层
  - 要么就是更接近图形字 / 旧字体字形那层
- 所以这一步已经从“翻译工程”变成了“显示工程”

### 这轮我试过但撤回的高风险实验
- 我试过直接把地图那层 `SWF` 的旧字体整块换成系统黑体。
- 结果很直接：
  - 冷启动会坏
  - 屏幕会黑掉或蓝掉
- 这条路目前不能直接当正式方案，所以我已经撤回，先保住正常可启动状态。

### 现在最诚实的结论
- `AS3`：
  - **全量文本翻译已完成**
  - **整包打包已完成**
  - **真实运行重新恢复正常**
  - **有声音**
  - **没有黑框弹窗**
  - **但首屏可见中文还没有验收通过**

### 这一轮一句话总结
- 现在已经不是“你怀疑我根本没在翻”，因为 `AS3` 的全量翻译和整包打包这次都已经真的跑完了。
- 现在真正卡住的，只剩下：
  - **那些已经翻好了、也打进包了，但在首屏上还没有被正确显示出来的旧 `SWF / 字体` 层。**

### 这轮我先把“越修越坏”的首页拉回来了
- 现在 `AS3` 首屏已经不再是：
  - 一片空白按钮
  - 一直卡在 `LOADING`
  - 或者字全丢了
- 我重新自己跑过闭环验收后，当前真实结果是：
  - 能稳定进到 `Poptropica - Flashpoint Navigator`
  - 首屏能看到完整的英文 `NEW PLAYER / RETURNING PLAYER`
  - 这次实际检测到了音频活动
  - 默认按钮链路里没有复现 `cmd / powershell / conhost` 黑框弹窗

### 这次我刻意做的取舍
- 我把启动页那批高风险中文补丁先从**默认运行包**里撤掉了。
- 原因很简单：
  - 它们之前没有真正稳定显示中文
  - 反而把首页按钮弄成空白、把启动流弄坏了
- 所以现在首页先回到**完整可见的英文**，保证你至少能正常看到、能正常点、能正常进。

### 现在的真实状态
- `AS3`：
  - 首屏已经恢复稳定
  - 有声音
  - 没复现黑框弹窗
  - **但首屏还没有通过“可见中文”验收**
- `AS2`：
  - 还能起窗口
  - 但这次自动验收抓到的画面还是不对
  - **声音和可见中文两项都还没过**

### 这轮不是“汉化完成”，而是“先把启动稳定性救回来”
- 我不再把“包里有中文资源”写成“用户已经看到中文”。
- 当前最诚实的结论就是：
  - `AS3` 已经从坏掉状态回到可启动、可见、可听
  - `AS2` 还没有过线
  - **真正稳定可见的中文，这一轮还没验收通过**

### 这轮我先不再瞎修游戏，而是先补了“闭环验收”
- 现在项目里多了一套本地验收工具。
- 它能自动做这些事：
  - 启动 `AS3` 或 `AS2`
  - 找到真实游戏窗口
  - 抓窗口截图
  - OCR 识别屏幕上到底有没有中文
  - 检查有没有 `cmd / powershell / conhost` 这类黑框弹窗
  - 检查声音有没有真的活动
- 这意味着后面我再修运行器、声音、地图卡死和汉化显示时，不再只是“改了然后猜”。

### 现在多了哪些可直接运行的命令
- `npm run qa:validate-as3`
- `npm run qa:validate-as2`
- `npm run qa:window-audit`
- `npm run qa:capture`
- `npm run qa:ocr`
- `npm run qa:audio-check`

### 这轮已经实际验出来的结果
- `AS3`：
  - 能进 `Poptropica - Flashpoint Navigator`
  - 没有在自动审计里复现可见的黑框弹窗
  - 第一屏**没有识别到中文**
  - 这次音频检测到了活动
- `AS2`：
  - 也能进 `Poptropica - Flashpoint Navigator`
  - 也没有在自动审计里复现可见的黑框弹窗
  - 第一屏**同样没有识别到中文**
  - 这次音频**没有**检测到明确活动

### 现在状态表和兼容性信息会更诚实
- 运行器兼容状态已经会写进 `runtime-data/player-compatibility.json`
- 不再只看“包里有没有补丁”，而是看真实验收结果
- 当前真实结论是：
  - `AS3` 还没通过闭环快验收
  - `AS2` 也还没通过闭环快验收

### 高级模式里现在也能直接跑验收
- 默认界面还是只保留普通用户需要的入口
- 但如果你按 `Ctrl + Shift + D` 进入高级模式，现在会多出：
  - `闭环验收 AS3`
  - `闭环验收 AS2`
  - `桌面窗口审计`

### 这一轮一句话总结
- 启动器现在不只是“会跑几个脚本”，而是已经能自己给出：
  - 有没有真的进游戏
  - 有没有真的看到中文
  - 有没有真的出现黑框
  - 有没有真的测到声音
- 但结果也很明确：
  - **两条链目前都还没有通过“首屏可见中文”这一关**
  - **AS2 的声音这一关也还没过**

## 2026-04-13

### 这次已经真正改掉的东西
- 默认界面已经重做了。
- 现在默认只保留三块：
  - 进入 AS3
  - 进入 AS2
  - 旧岛状态表
- 原来那种“在启动器外面一张张卡片直接跳岛”的默认玩法已经拿掉了。
- 现在的正常用法改成：
  - 打开启动器
  - 进入 AS3 或 AS2
  - 在游戏里面登录、进岛、切岛

### 界面现在会是什么样
- 默认页已经不是之前那种挤在一起的大卡片墙了。
- 现在是一个更干净的主界面：
  - 顶部状态
  - 中间两个主要入口
  - 下方旧岛状态表
- 高级模式还在，但默认隐藏了。
- 只有按 `Ctrl + Shift + D` 才会把调试区打开。

### 我已经亲自跑过的检查
- 启动器可以正常打开。
- 我抓了新的启动器截图，默认界面已经换成新布局。
- UI 自动检查通过了：
  - 标题可见
  - `进入 AS3` 按钮可见
  - `进入 AS2` 按钮可见
  - `重建汉化包` 按钮可见
  - 高级区默认隐藏
  - 默认界面里已经没有“外部直启岛屿”
- 窗口审计也跑过了：
  - 当前默认链路下，没有可见的 `cmd / powershell` 弹窗

### 运行器这次怎么改了
- `AS2` 和 `AS3` 现在不再强行走同一条运行链。
- `AS2` 已经改成优先走更适合旧 Flash9 的链路。
- `AS3` 也改成优先走浏览器型链路，而不是继续死绑以前那条旧链。
- 这一步的目标很明确：
  - 先把 AS2 那种卡成逐帧的问题拆出来单独治
  - 再给声音和图形稳定性留出正确的修复入口

### 状态表现在更诚实了
- 以前状态表里会出现“看起来已经汉化好了”，但实际上游戏里根本没看到中文。
- 现在状态表改成更保守的写法了。
- 默认只会告诉你：
  - 这个岛是 AS2 还是 AS3
  - 现在是不是待验证
  - 汉化是未提取、已提取待翻译，还是已打包未验收
- 也就是说，现在不会再轻易把“包里有补丁”直接吹成“已经可见中文”。

### 现在还没收尾的地方
- 声音问题还没有被我正式验收通过。
- 我还没有把 `Virus Hunter`、`Mocktropica` 和一个稳定的 AS2 基准岛做成“已验收可见中文”。
- 也就是说：
  - 启动器和默认用户流现在已经比之前顺很多
  - 但“旧版游戏里第一次进去就稳定听到声音、稳定看到中文”这件事还在继续追

### 这轮新确认出来的关键问题
- 我已经把真实运行截图重新跑过了。
- 结果不是“好像没翻到”，而是确实还在显示英文：
  - `NEW PLAYER`
  - `ARE YOU A BOY OR A GIRL?`
  - `BOY`
  - `GIRL`
- 这一步我已经不用肉眼猜了，而是直接做了实际点击、截图和文字识别。

### 这轮真正修掉的一个大坑
- 我找到一个非常关键的问题：
  - 之前用来修开始页的那个运行包，
  - 实际上是从原始 `AS3.zip` 重新做出来的，
  - 结果把前面已经做好的整包汉化内容又覆盖掉了。
- 这就是为什么会出现：
  - 数据库里看着像翻过了
  - 但你进游戏几乎看不到中文
- 现在这条链已经修掉了：
  - 登录页修补会基于**完整汉化运行包**继续叠加
  - 不会再把整包汉化内容冲掉

### 我这轮已经实际验证过的事
- 运行器真实拿到的 `login.swf` 现在确实已经是中文内容了。
- 我还确认了：
  - 这个中文版 `login.swf` 已经真的被挂到 Flashpoint 的运行包里
  - 浏览器请求它时，拿到的也确实是中文版本

### 为什么画面现在还是英文
- 原因已经缩小了。
- 现在不是“中文资源没送到”。
- 现在更像是：
  - 你看到的那几个开始页英文按钮和标题
  - 并不是普通可替换文本
  - 而是更偏图形/矢量那一层出来的
- 换句话说：
  - 之前那套文本补丁链已经打到普通文本了
  - 但开始页这几个最显眼的英文，还需要单独走图形级处理

### 我接下来正在追的方向
- 第一件事：
  - 继续把开始页这几处英文做成真正可见的中文
- 第二件事：
  - 继续验证完整汉化运行包进游戏后，场景内文本到底哪些已经正常显示中文
- 第三件事：
  - 继续查声音为什么资源已经在请求，但你体感上还是没有声音

### 一句话总结
- 现在已经不是之前那个“默认界面乱、外部直跳一堆、按钮后面藏一堆调试玩意”的状态了。
- 现在至少已经变成了一个更像正常产品的启动器。
- 下一步我继续盯的是三件事：
  - 声音
  - AS2 实际流畅度
  - 真实可见中文的验收

## 2026-04-13 晚上追加

### 这轮我又确认并修掉了什么
- 我把“打包目录里的中文是新的，但 Flashpoint 真正运行的还是旧包”这个问题重新追了一遍。
- 现在已经确认：
  - 真正挂给 Flashpoint 的运行包已经同步到最新版本
  - 不再是“仓库里中文是对的，实际跑起来还是旧英文包”

### 这次已经写进实际运行包里的内容
- AS3 起始页那批关键中文已经重新写进实际运行包了。
- 现在运行包里的起始页文本已经是：
  - 新玩家
  - 继续游戏
  - 你是男孩还是女孩?
  - 男孩 / 女孩
  - 你几岁了?
  - 全部重选 / 更换颜色 / 导入形象 / 完成
- 这些字现在也不再继续沿用原来那个只适合英文的字体名了。
- 我把它们统一改成了更适合中文显示的黑体链。

### AS2 / AS3 这轮真正分开了什么
- 之前嘴上说分流，实际上两边还是在吃同一套 Flash 插件。
- 现在不是了：
  - AS3 进入时会切回 Flash32
  - AS2 进入时会切回旧 Flash9 链
- 也就是说，这次开始：
  - AS2 不再继续被强喂新版插件
  - 我终于能单独处理 AS2 那个“又慢又不稳”的问题

### 我这轮亲自复跑过的检查
- `doctor` 重新跑过了，运行环境还是正常的。
- `launch --runtime as3` 重新跑过了。
- `launch --runtime as2` 重新跑过了。
- 默认按钮弹窗检查也又跑过了一次：
  - 当前默认链路下，仍然没有可见的 `cmd / powershell` 弹窗

### 这轮新确认出的现实情况
- 我这边的自动截图，现在仍然很容易只截到 Flash 外壳层，不一定能完整反映屏幕上真正显示的那一层。
- 但日志已经能确认：
  - 起始页场景确实加载到了
  - 中文起始岛文本也确实进入了运行链
- 所以这一步现在不再是“中文资源根本没进去”，而是“还要继续把真实可见效果验收做扎实”。

### 现在还没彻底解决的两件大事
- 声音还没有正式验收通过。
- AS2 虽然已经切回更合适的旧插件链，但我还没把它正式验收到“真的顺畅可玩”。

### 这轮一句话总结
- 现在最关键的运行包同步问题已经修通了。
- AS2 / AS3 的 Flash 插件链也终于真正拆开了。
- 下一步继续硬追两件事：
  - 屏幕上真实可见的中文验收
  - 声音和 AS2 体验

## 2026-04-15 深夜追加

### 这次不再全量乱跑，改成只盯 `Super Power`
- 我这次停掉了那种一跑几个小时、但你看不到结果的全量任务。
- 改成只处理一个岛：`Super Power Island`
- 先把这个岛跑通，再谈扩到全游戏。

### 先说清楚一个关键点
- `Super Power` 不是 AS3。
- 它属于 `AS2` 旧岛。
- 所以这次做的是：
  - 只针对 `AS2`
  - 只针对 `Super Power`
  - 不再拿全游戏一起陪跑

### 这次已经真的做成了什么
- 我已经把 `Super Power` 单岛需要的那批资源单独提出来了。
- 这次提出来的量是：
  - 25 个目标资源
  - 275 条文本
- 然后我把这一岛的补丁重新打进了实际运行包。
- 现在给 `AS2` 用的运行包已经是新的：
  - `E:\Poptropica\POPTROPICA_FLASH\runtime-data\patched-zips\as2-runtime.zip`

### 这次和以前最大的区别
- 以前很多时候是：
  - 数据库里有内容
  - 但实际运行包没更新
  - 所以你进游戏还是旧东西
- 这次不是了。
- 这次 `Super Power` 的补丁已经真的进了 `AS2` 运行包。

### 目前这一步已经成功的部分
- `Super Power` 的单岛提取已经完成。
- `Super Power` 的单岛翻译记忆已经灌进去了。
- `Super Power` 的单岛运行包已经重新打好了。
- 当前成功写进去的 `SWF` 补丁有 13 个。

### 目前还没完全打通的部分
- 还有 3 个 `SWF` 文本回写失败了。
- 也就是说：
  - 现在不能说这个岛已经 100% 全汉化
  - 但它已经不是“什么都没做”的状态
- 这 3 个失败点是：
  - 地图页相关
  - 一份新闻页相关
  - 一份银行场景相关

### 你现在怎么测试
- 如果你想直接测 `Super Power`，最直接的是在项目目录里运行：

```powershell
cd E:\Poptropica\POPTROPICA_FLASH
npm run launch -- --island super-power
```

- 如果你想先开启动器，再自己进旧版游戏，也可以双击：
  - `E:\Poptropica\POPTROPICA_FLASH\Start-Poptropica-Flash.vbs`
  - 或 `E:\Poptropica\POPTROPICA_FLASH\Start-Poptropica-Flash.bat`

### 这一轮一句话总结
- 这次终于不是“全量乱跑但没结果”了。
- 这次是第一次把 `Super Power` 真的单独拆出来构建，并且把补丁打进了实际 `AS2` 运行包。
- 下一步就继续只追这个岛，把剩下那 3 个失败点干掉，再看屏幕上的真实中文效果。

## 2026-04-16 早上追加

### `Super Power` 这次真正修到了哪一步
- 我没有再继续跑全游戏。
- 这次只盯 `Super Power Island`。
- 这次已经把它最后卡住的那几个 `SWF` 注入点逐个拆开修掉了。

### 之前为什么总差最后一点
- 之前不是“没翻译”，而是卡在老 Flash 的 `SWF` 回写上。
- 主要有三类坑：
  - AS2 这条链更适合用 `plain text` 回写，不适合整批 `formatted` 硬灌
  - FFDec 对文本文件的换行格式很挑，必须是 Windows 风格的 `CRLF`
  - 同一个 `SWF` 里不同标题块，有时甚至不是同一个字体 ID，不能整包一把替

### 这次我具体修掉了什么
- `sceneBank.swf` 已经修好。
- `maps/Super.swf` 已经修好。
- 最后卡住的 `superheadlines.swf` 也已经单独打通并塞回正式运行包了。
- 现在 `Super Power` 这一岛在构建层面已经不再有待处理的 `SWF` 失败项。

### 现在的构建结果
- `assetsPatched = 15`
- `pendingSwfAssets = 0`
- `runtime zip replacementCount = 16`
- 也就是说：
  - 这次不再是“14 个成功、1 个失败”
  - 而是 `Super Power` 这批单岛补丁已经完整进包

### 现在怎么启动测试
- 直接在项目目录里运行：

```powershell
cd E:\Poptropica\POPTROPICA_FLASH
npm run launch -- --island super-power
```

- 这条命令现在已经能把 `Super Power` 的最新运行包挂上去。

### 这一轮一句话总结
- 现在 `Super Power` 已经不是“构建失败中的半成品”了。
- 从补丁、运行包、挂载这三层看，这个岛的注入链已经打通。

## 2026-04-16 下午追加

### 这次真正修掉了“命令说启动成功，但屏幕啥也没有”
- 之前 `Super Power` 这条链卡住，不是因为岛资源没挂上。
- 真正的问题有两个：
  - 启动参数里把这个岛写成了 `SuperPower`
  - 但实际资源路径用的是 `Super`
  - 启动器默认走的 `FPNavigator.exe` 这一层自己也不稳定，容易卡死或拦住新窗口

### 这次我改掉了什么
- 我把 `Super Power` 的启动参数从错误的 `SuperPower` 改成了真正资源对应的 `Super`
- 我把正式启动链改成：
  - 启动前先清掉旧的 `FlashpointNavigator`
  - 然后直接用底层的 `flashpointnavigator.exe` 打开游戏
- 这样做以后：
  - 不会再被那个 `Close FlashpointNavigator` 弹框拦住
  - 也不会再出现“命令返回成功，但实际上窗口没起来”

### 这次我亲自验到的真实结果
- 我重新启动了 `Super Power`
- 现在后台日志已经明确显示：
  - 真正加载到了 `Super Power Island`
  - 场景是 `Main Street`
- 我还直接抓了真实窗口截图：
  - `E:\Poptropica\POPTROPICA_FLASH\runtime-data\qa\super-power-window.png`
- 截图里已经不是空白页，而是已经进到 `Super Power` 的街道画面

### 你现在怎么测
- 直接运行：

```powershell
cd E:\Poptropica\POPTROPICA_FLASH
npm run launch -- --island super-power
```

### 你现在应该看到什么
- 会打开 `Poptropica - Flashpoint Navigator`
- 不是空白页
- 会直接进到 `Super Power Island` 的主街

### 这轮一句话总结
- 这次终于把“能挂载资源”和“真的把窗口拉起来并进岛”这两件事连上了。
- 现在 `Super Power` 至少已经从“空白 / 没反应”推进到了“真实进岛可见画面”。
# 2026-04-17 01:05

- 修了 `Super Power` 纯蓝页的根因。
- 这次不是你点错了，也不是资源包没挂上，而是本地 PHP 路由服务没真正活着，游戏虽然打开了 `base.php`，但后续老资源链断掉了，所以最后只剩一整页蓝底。
- 现在启动前会先确认本地 PHP 路由真的监听在 `22600`，没起来就直接报错，不再假装成功。
- 我已经重新实测过：当前运行状态里 `proxy=true`、`zip=true`、`php=true`，而且游戏服务器日志里也不再是“连 22600 被拒绝”那种错误。
- 现在如果还出问题，最该看的日志是：
  - `E:\Poptropica\POPTROPICA_FLASH\runtime-data\workspaces\flashpoint-managed\logs\flashpoint-game-server.log`
  - `E:\Poptropica\POPTROPICA_FLASH\runtime-data\workspaces\flashpoint-managed\logs\flashpoint-php-router.log`
  - `E:\Poptropica\POPTROPICA_FLASH\runtime-data\flashpoint-runtime-state.json`

# 2026-04-17 02:10

- 修了旧版浏览器 profile 里一组会反复报错的坏配置。
- 之前实际在用的 `prefs.js` 里还残留着两类坏值：
  - `media.gmp-manager.url = ""`
  - `plugin.disable_full_page_plugin_for_types = 2`
- 这会让浏览器一直报 `GMPInstallManager ... URI is malformed` 之类的错误，干扰页面启动和排查。
- 现在启动链会更强硬地覆盖这些坏配置，我也已经把你当前正在用的 profile 直接修正了一次。
- 这次修完后，相关配置已经变成：
  - `plugin.disable_full_page_plugin_for_types = ""`
  - `pdfjs.disabled = true`
  - `media.gmp-manager.updateEnabled = false`
  - `media.gmp-provider.enabled = false`
  - `media.gmp-manager.url = "http://127.0.0.1/flashpoint-gmp-dummy.xml"`
  - `media.gmp-manager.url.override = "http://127.0.0.1/flashpoint-gmp-dummy.xml"`

# 2026-04-17 03:25

- 这轮没有把 `Super Power` 端到端彻底修好，但抓到了两个真正关键的新病根。
- 第一个病根：我之前把 `Super Power` 主街脚本里的 `roomName` 也翻成了中文 `主街`。这个字段不是给玩家看的，而是场景内部识别用的。翻掉以后，旧版引擎会把房间判成无效，然后跳到 `SORRY, THAT ROOM IS NOT AVAILABLE RIGHT NOW`。
- 我已经把这个字段改回英文 `Main Street`，并重新打进实际运行包。
- 第二个病根：靠追着会移动的 NPC 点，根本不适合验证对话链。所以这轮我改成了“主街自动哨兵对白”方案：进入 `Super Power` 主街后，会尝试自动给玩家自己弹一个固定对白 `@@SP1@@`，同时我还加了一层固定字幕条，不再完全依赖旧气泡。
- 这些补丁已经确认在真正运行的 `as2-runtime.zip` 里了，不是只停留在脚本目录里。
- 当前真正还没过的阻塞项是：
  - `Super Power` 的直跳入口仍然会先掉一次错误页
  - 所以自动对白有时会在错误页阶段被浪费掉
  - 这也是为什么你现在看到的现象还是“不稳定、像没生效”
- 这一轮最重要的结果不是“看起来好像又改了一堆”，而是终于把问题从“可能是字体/可能是对话/可能是运行器”缩小到了：
  - `AS2` 直跳入口本身不稳
  - `Super Power` 主街对白链已经被我接管到固定字幕层
  - 下一步只需要继续追 `Super Power` 的入口稳定和场景实际落点，不再回去大范围乱修
